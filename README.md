# login-invite-reset

This is a boilerplate application for an early stage, invite only web-app. It features a session-based authentication system using Passport-local and also includes an admin panel where authorized users can invite new users and reset their password using the Sendgrid API.